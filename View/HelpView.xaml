<UserControl 
    x:Class="CTRI.View.HelpView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    mc:Ignorable="d"
    d:DesignHeight="470" d:DesignWidth="950">
    <UserControl.Resources>
        <Style TargetType="ToolTip" BasedOn="{StaticResource RD_ToolTip}"/>
    </UserControl.Resources>
    <Grid >
        <DocumentViewer Name="DocumentViewer_Help" Foreground="{StaticResource BlueColor}" Background="{StaticResource WathetColor2}"  BorderBrush="{StaticResource BlueColor2}" BorderThickness="1" />
    </Grid>
</UserControl>
